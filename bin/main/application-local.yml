server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/chat_space_db
    username: chatdev
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

# p6spy: 실제 파라미터가 바인딩된 완성형 SQL 로그 활성화
logging:
  level:
    p6spy: info

file:
  upload-dir: C:/ClaudeCode/uploads
